[phases.setup]
nixPkgs = ["nodejs", "npm"]

[phases.install]
cmds = [
    "cd twclaw && npm install && npm link"
]

[start]
cmd = "cd backend && python -m uvicorn main:app --host 0.0.0.0 --port $PORT"
